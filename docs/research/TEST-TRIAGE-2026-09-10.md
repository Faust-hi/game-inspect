# Отбор проверок по последствиям ошибки (исследование, 10.09.2026)

Документ — результат исследовательского этапа плана. **Изменений в код и тесты
не вносилось**: только инвентаризация, разбор по дефектам и предложения.

---

## 1. Что уже сделано до этого исследования

Коммит `551ae62` (10.09.2026, 09:04) уже выполнил основную часть плана:
удалено 27 тестовых и инфраструктурных файлов (`test_api.py`,
`test_calibration.py`, `test_hardware_semantics.py`, `test_profile_influence.py`,
`test_publication.py`, `test_entity_lifecycle.py`, `test_db_migrate.py`,
`test_static_security.py`, `smoke_check.py` и др.), создано 9 файлов критических
свойств, переписаны два браузерных сценария, обновлены README и CI.

Среда приведена к состоянию этого коммита и проверена (см. §7).

## 2. Инвентарь

| Контур | Где | Что | Прогон |
|---|---|---|---|
| Офлайн backend | `backend/tests/` (9 файлов) | критические свойства предметной области | **58 тестов, все зелёные**, 5.5 с |
| Офлайн frontend | `frontend/src/__tests__` (7 файлов) | состояние экрана и разбор ошибок | **29 тестов, все зелёные**, 2.1 с |
| Офлайн, внешняя сверка | `validation/test_evaluate.py` | метрика ±70 %, пропуски, train/test | **6 тестов, все зелёные** |
| Офлайн браузер | `frontend/e2e/` (2 сценария) | основной путь и видимость конфликта | вручную, перед показом |
| Активный | `app/main.py`, `app/db_migrate.py`, обработчики ошибок | предохранители при старте и расчёте | при каждом запуске |

Распределение backend-тестов по файлам (после правок): `test_decision_math` 9,
`test_combined_effects` 9, `test_hardware_bottleneck` 8, `test_user_journey` 6,
`test_scientific` 6, `test_demo_stability` 6, `test_project_context` 5,
`test_engine_versions` 5, `test_conflicts` 4.

## 3. Активный контур (§2 плана) — состояние

Присутствует и соответствует требованиям плана:

* **Старт** (`_bootstrap`): миграции вместо `create_all`, отказ при неподдерживаемой
  СУБД, первичное заполнение пустой базы, синхронизация классификации.
* **Готовность** (`/api/health`): `ready` / `degraded` / `unavailable`, отдельно —
  `schema_error`, счётчики опубликованных методов и железа.
* **Расчёт**: неизвестные коды → риски, исключённые решения — с причиной,
  конфликты/зависимости/синергии корзины, `unmet_limits`, `modeling_gaps`,
  `confidence` < 1 и `caveats`, `exceeds_catalog`.
* **Ошибки**: единый формат `{error, details, code, request_id}` плюс заголовок
  `x-request-id`; подробности исключений клиенту не передаются.

Полный pytest и браузер при обычном запуске не выполняются — это зафиксировано
и в коде, и в README.

## 4. Покрытие критических областей (§3 плана)

| Область | Файл | Примеры различаются по смыслу |
|---|---|---|
| Математика решений и ранжирование | `test_decision_math` | доминирующая альтернатива; вес меняет порядок; вырожденная матрица; воспроизводимость; конечность балла; вилка устойчивости |
| Подбор железа и узкие места | `test_hardware_bottleneck` | разрешение; FPS; дефицит памяти; ложное узкое место; альтернативы не слабее оценки; неизвестность вместо обещания; выход за границу модели |
| Движок и версии | `test_engine_versions` | неизвестный движок; движок из каталога без правки кода; версия срезает инструмент; Linux+DX12; платформенное условие DirectStorage |
| Контекст игры и этап | `test_project_context` | поле анкеты меняет результат; стадия — цена, а не удаление; своё содержание у каждой стадии; смена замысла; сетевой метод только с сетевой функцией |
| Совместное действие методов | `test_combined_effects` | пул стриминга; поле и карточка — одно действие; RT дорожает с разрешением; бюджет трассировки; растр+RT делят бюджет кадра; сложение расходов, но не экономий; серверная скидка не игроку; эффект вне кадра назван |
| Конфликты и зависимости | `test_conflicts` | конфликт виден в корзине; незакрытая зависимость; синергия; жёсткая несовместимость не складывается |
| Сквозной сценарий | `test_user_journey` + `e2e/project.spec.ts` | ранг и причины; неизвестные коды; объяснённые исключения; корзина → нагрузка → железо; воспроизводимость; зафиксированная основа |
| Стабильность показа | `test_demo_stability` + `e2e/` | health, чистая SQLite, целостность опубликованного среза, черновик не протекает, понятная ошибка |
| Научная корректность | `test_scientific` + `validation/test_evaluate.py` | «в разработке», а не доказанная точность; пропуск — не успех; внешний якорь по картам; метрика и разделение выборок |

## 5. Найденные дефекты самих проверок

### 5.1 Вакуумная проверка целостности каталога

`backend/tests/test_demo_stability.py`, `test_published_slice_has_sources_and_no_dangling_links`:

```python
assert link["a_code"] in codes or link["a_code"], link
assert link["b_code"] in codes or link["b_code"], link
```

При непустом коде второе условие истинно всегда, поэтому оборванная связь
конфликта **не обнаруживается**. Инвариант из §4 плана заявлен, но не работает.
Требуется переписать на строгое вхождение в множество кодов.

### 5.2 Тест, копирующий формулу реализации

`test_combined_effects.py::test_raster_and_rt_share_one_frame_budget` сравнивает
`required_gpu_index` с `(raster + rt) / budget` — это формула реализации.
По §3 плана ожидаемое должно следовать из независимого правила: «требование к
GPU учитывает оба прохода, а не только более тяжёлый».

### 5.3 Проверка приватной функции

`test_hardware_bottleneck.py::test_zero_means_empty_not_missing` вызывает
`app.services.hardware._load_indices`. Свойство «явный ноль дешевле
неизвестности» существенно, но проверять его нужно через API.

## 6. Предложения по дальнейшему сокращению

### Исправить (без изменения числа тестов)

1. §5.1 — строгая проверка связей конфликтов.
2. §5.2 — переписать через независимое свойство.
3. §5.3 — перевести на API (`/api/hardware-estimate` с `npc_count=0` против
   `npc_count_level="unknown"`).

### Объединить (несколько тестов на один дефект)

4. `test_decision_math`: `test_single_alternative_is_neutral_not_worst` и
   `test_identical_alternatives_are_incomparable` — один дефект «вырожденное
   сравнение помечено худшим». Объединить в один параметризованный тест.
   Счётчик pytest не уменьшится (два случая), но это разные ветки: `n == 1`
   и «все строки равны». Устранён дубль проверки, а не случай.
5. `store.test.tsx`: цикл из шести однотипных тестов
   (`updateProfile`/`toggleBasket`/`setBasket`/`clearBasket`/`resetProfile`/
   `loadProject`) проверяет один инвариант — прежний результат сбрасывается при
   изменении входа. Оставить один параметризованный. **−5**

### Удалить (§4 плана)

6. `test_demo_stability::test_unknown_structure_is_refused_with_backup` —
   глубокая инфраструктура миграций; планом оставлена только минимальная
   проверка чистой SQLite. **−1**
7. `test_demo_stability::test_static_path_rejects_escape_and_serves_nested` —
   инфраструктура безопасности. **−1**
8. `test_demo_stability::test_foreign_origin_mutation_is_rejected` —
   инфраструктура безопасности. **−1**
   Сохраняется `test_critical_error_is_understandable`: это офлайн-проверка
   предохранителя из §2 плана.
9. `store.test.tsx`: «отменяет выполняющийся запрос при изменении входа»
   проверяет `AbortSignal` — деталь реализации; наблюдаемое поведение уже
   покрыто тестом «поздний ответ не перезаписывает актуальный результат». **−1**
10. `store.test.tsx`: «каталог перезагружается — устаревший ответ отменяется»
    дублирует тот же тест о позднем ответе. **−1**
11. `api.test.ts`: из шести проверок разбора ошибки оставить четыре —
    единый формат, fallback `{detail}`, приведение `body.`-префикса, код
    обращения при 5xx. Убрать проверку `instanceof ApiRequestError` и вариант
    «без тела ответа» (деталь класса и пограничный случай без последствий). **−2**

Факт после правок (подтверждено прогоном): backend **61 → 58**,
frontend **38 → 29**. Численного лимита план не требует — это следствие
разбора, а не цель.

## 7. Мутационная проверка полезности (§6 плана) — выполнена

Каждая ошибка вносилась в продукт, запускался соответствующий файл тестов,
затем правка откатывалась (`git checkout --`). Итог:

| № | Внесённая ошибка | Где | Упавшие тесты |
|---|---|---|---|
| 1 | Перевёрнуты `benefit`/`cost` | `services/topsis.py` | `test_dominant_alternative_ranks_first`, `test_weights_flip_order`, `test_clear_winner_is_stable_and_tie_shows_fork` |
| 2 | Веса критериев игнорируются | `services/topsis.py` | `test_weights_flip_order`, `test_priority_changes_api_ranking_not_just_weights`, `test_clear_winner_is_stable_and_tie_shows_fork` |
| 3 | Жёсткая несовместимость не исключает решения | `services/rules.py` | `test_hard_conflict_blocks_stacking` |
| 4 | Пул стриминга учтён полностью | `services/hardware.py` | `test_streaming_pool_counts_only_transient_part` |
| 5 | Версия движка не проверяется | `services/engines.py` | `test_version_cuts_builtin_tool_and_warns_in_basket` |
| 6 | Неизвестная функция не помечена риском | `services/recommender.py` | `test_missing_prediction_does_not_become_success`, `test_unknown_codes_become_risks` |
| 7 | Оборванная связь в каталоге конфликтов | запись `Conflict` со статусом `published` | только исправленная проверка; прежняя формулировка проходит |

Мутации 1 и 2 ловятся более чем одним тестом — это допустимое перекрытие:
первый тест проверяет порядок на малой матрице, второй — влияние приоритета
через API, и это разные дефекты (инверсия шкалы против потери веса).

Мутация 7 подтвердила цену дефекта §5.1: при оборванной связи прежняя
формулировка `код in коды or код` остаётся истинной, исправленная — падает.

## 8. Прочее по плану

* **Команды запуска и CI.** `ci.yml` выполняет backend (pytest, миграции,
  `alembic check`), внешнюю сверку `validation/`, frontend (типы, тесты, сборку).
  Браузерные сценарии запускаются вручную перед показом — это соответствует
  §5 плана. Отдельного задания для них не требуется.
* **Документация.** README раздел 9 уже описывает два контура, таблицу файлов и
  фактические числа прогонов (61 и 38). После правок §6 нужно обновить числа и
  таблицу.
* **Память проекта.** Записанное в памяти число «452 теста» устарело: актуально
  61 backend / 38 frontend / 6 внешняя сверка.

---

# Часть 2. Проверка вне backend (интерфейс, браузер, документация)

Правки продукта не вносились: только фиксация расхождений.

## П2.1 Мёртвый интерфейс

Сверка выполнена так: поднималась временная база, заполнялась сидом, снимались
реальные ответы API, затем объявленные поля `frontend/src/types.ts`
сравнивались с фактическими ключами ответов.

| Тип | Объявлено | Пришло | Расхождение |
|---|---|---|---|
| `RecommendationResult` | 20 | 20 | — |
| `Recommendation` | 29 | 29 | — |
| `Risk` | 5 | 5 | — |
| `HardwareEstimate` | 33 | 33 | — |
| `LoadProfile` | 8 | 8 | — |
| `GameFunction` | 9 | 9 | — |
| `Method` | 48 | 47 | `used_in_projects` |

**Находка 1 — `used_in_projects` (мёртвая ветка).**
Объявлено в `types.ts:134`, используется в `MethodCard.tsx:146-148` (блок
«Где применено»). В backend поля нет вообще: ни в моделях, ни в схемах, ни в
сериализаторах — 0 упоминаний. Ветка всегда ложна, блок никогда не показывается.

**Находка 2 — тип `GameExample` целиком мёртв.**
`types.ts:195-217`, 22 поля (`year`, `developer`, `optimizations_used`,
`performance_outcome`, `verified_by` и др.). Таблица `game_examples` удалена
миграцией (`db_migrate._LEGACY_REMOVED_TABLES`), endpoint отсутствует, в
компонентах тип не используется. Удаление сущности не было доведено до типов.

Больше расхождений нет: из 302 полей типов только 5 не находятся в backend, и
все 5 относятся к этим двум случаям.

## П2.2 Браузерный контур проверяет устаревшую сборку

`e2e_server.py` поднимает `app.main:app`, который раздаёт `frontend/dist`.
Сборка в репозитории была от 09.09 14:46 при исходниках от 10.09 09:06 —
текста «Зафиксировать корзину как реализованную» в бандле не было.

* На устаревшей сборке основной сценарий `project.spec.ts` **падает**:
  `getByRole('button', {name: /Зафиксировать корзину как реализованную/})`
  не найден (timeout 5 c).
* После `npm run build` оба сценария проходят: **2 passed** (1.9 c и 2.1 c).

То есть браузерная проверка молча тестирует старый код. Перед показом
обязательна пересборка; иначе контур не защищает, а создаёт ложную уверенность.
Стоит либо пересобирать в команде запуска, либо гонять сценарии против
dev-сервера.

Отдельно: в этой среде браузер отсутствовал (`@playwright/test` 1.63 требует
сборку 1243, были 1228/1234) — доставлен командой
`npx playwright install chromium`.

## П2.3 SPA-fallback перехватывает неизвестные пути API

При собранном `dist` регистрируется маршрут `/{full_path:path}`, поэтому
`GET /api/catalog/nonexistent` возвращает **200 text/html** — главную страницу
приложения, а не 404. Ошибка в адресе запроса маскируется: клиент получает
«успешный» ответ, который не удаётся разобрать. `POST` на неизвестный путь
даёт корректный 405 JSON, то есть проблема только для GET.

## П2.4 README разошёлся с фактическим наполнением

Факт снят с заполненной базы (подсчёт по моделям и по публичному каталогу):

| Показатель | README | Факт |
|---|---|---|
| Игровых функций | 28 | **40** |
| Методов и вариантов реализации | 95 | **124** |
| Движков | 7 | 7 |
| Примеров игр в публичном каталоге | 135 | **0 — сущность удалена** |
| Черновиков Трека 2 (вне рекомендаций) | 71 | **0** |
| Процессоров | 51 | 51 |
| Видеокарт | 79 | 79 |
| Конфликтов и зависимостей | 27 | **37** |
| Инструментов движков | 67 | **70** |
| Связей «метод — инструмент» | 388 | **473** |

У всех 124 опубликованных методов заполнен `source_url` (пропусков нет).

## П2.5 Замечания целостности после заполнения

Сид оставляет 13 предупреждений «У метода нет связей с инструментами движков».
Это 10 % каталога: у таких методов в рекомендациях `engine_support = null`,
то есть привязка к версии движка для них не показана. Не блокирующее, но
заметно в выдаче.

## П2.6 Мелкое

* Неиспользуемых модулей во frontend нет (кроме точки входа `main.tsx`).
* `TODO` / `FIXME` в `frontend/src` и `backend/app` нет; единственная
  содержательная пометка — заглушка сверки с практикой
  (`schemas/catalog.py:681`), она же осознанно проверяется тестом
  «в разработке, а не доказанная точность».

---

# Часть 3. Исполнение §6 и повторная мутационная проверка

## П3.1 Выполненные правки

Все одиннадцать пунктов §6 внесены (изменения до этой части уже лежали
в рабочем каталоге незафиксированными):

| Пункт | Что сделано | Проверено |
|---|---|---|
| 1 (§5.1) | строгие `link["a_code"] in codes` / `b_code` | мутация 7 из §7 |
| 2 (§5.2) | границы «больше любого прохода, не больше суммы» вместо формулы | две мутации |
| 3 (§5.3) | `npc_count=0` против `npc_count_level="unknown"` через `/api/hardware-estimate` | мутация |
| 4 | два вырожденных случая объединены в параметризованный тест | мутация |
| 5 | шесть однотипных тестов `store.test.tsx` — один цикл с именем действия | мутация |
| 6, 7, 8 | удалены глубокая миграция и две инфраструктурные проверки | прогон |
| 9, 10 | удалены проверка `AbortSignal` и дубль «поздний ответ» | прогон |
| 11 | `api.test.ts`: 6 → 4, убраны `instanceof` и «без тела ответа» | прогон |

Итог: backend **61 → 58**, frontend **38 → 29**.

## П3.2 Повторная мутационная проверка переписанных тестов

Переписанный тест мог потерять силу, не упав ни разу, поэтому мутации
§7 выполнены заново именно для изменённых проверок. Каждая ошибка вносилась
в продукт и откатывалась `git checkout --`.

| № | Внесённая ошибка | Где | Упавший тест |
|---|---|---|---|
| 1 | GPU: учтён только самый тяжёлый проход (`max` вместо суммы) | `services/hardware.py` | `test_raster_and_rt_share_one_frame_budget` |
| 2 | GPU: работа учтена дважды (`2 * (raster + rt)`) | `services/hardware.py` | тот же тест |

Ошибки 1 и 2 показывают, что **обе** границы независимого правила
рабочие: прежняя формулировка копировала реализацию и не различала бы
эти случаи по отдельности.

| № | Внесённая ошибка | Где | Упавший тест |
|---|---|---|---|
| 3 | Явный ноль приравнен к неизвестности | `schemas/catalog.py` | `test_zero_means_empty_not_missing` |
| 4 | Сняты предохранители вырожденного сравнения | `services/topsis.py` | `test_degenerate_comparison_is_neutral_not_worst` (оба случая) |
| 5 | Убран сброс результата в `toggleBasket` | `frontend/src/store.tsx` | «сбрасывает прежний результат при любом изменении входа» |

Мутация 5 подтвердила, что объединение шести тестов в один не стоило
диагностики: сообщение о падении содержит имя сломанного действия
(`toggleBasket: expected {…} to be null`).

## П3.3 Исправленные дефекты продукта (из части 2)

В план это не входило, но оба дефекта обнаружены исследованием части 2
и закрыты, так как правки точечные.

* **П2.3, SPA-fallback маскировал неизвестные пути API.** В `app/main.py`
  маршрут `/{full_path:path}` теперь отвечает 404 в едином формате
  (`{error, code, request_id}`) на пути, начинающиеся с `api/`; прочие пути
  по-прежнему отдают страницу. Сверено: `/api/catalog/nonexistent` →
  404 `application/json`, `/api` → 404 JSON, `/profile` → 200 `text/html`,
  `/api/health` → 200 JSON. Добавлен тест
  `test_unknown_api_path_is_not_served_as_page`; без правки он падает
  (`assert 200 == 404`).
* **П2.1, мёртвый интерфейс.** Удалены поле `used_in_projects`
  (`types.ts`, ветка «Примеры применения» в `MethodCard.tsx`) и целиком
  неиспользуемый тип `GameExample` (22 поля удалённой сущности).
* **П2.2, браузерный контур проверял устаревшую сборку.** Сборка выполняется
  в `globalSetup` до подъёма сервера, поэтому любой способ вызова безопасен.
  Пересборка только при устаревании `dist` относительно исходников: полная
  сборка очищает каталог и идёт около секунды, а прогон запускают часто.
  Добавлен сценарий `npm run test:e2e`. Обе ветви проверены: на свежей сборке
  выводится «пересборка не требуется», при `touch src/main.tsx` — сборка
  запускается.
  Устройство выбрано после неудачной попытки: первая версия собирала всегда,
  и `__dirname` оказался недоступен — при `"type": "module"` Playwright
  загружает `globalSetup` как ES-модуль, путь получен через
  `fileURLToPath(import.meta.url)`.

## П3.4 Фактическое состояние после правок

| Контур | Команда | Результат |
|---|---|---|
| Backend | `pytest -q` | **59 пройдено** (58 по §6 плюс один на П2.3) |
| Внешняя сверка | `pytest -q` в `validation/` | **6 пройдено** |
| Frontend | `npm test` | **29 пройдено** |
| Типы frontend | `npm run typecheck` | без ошибок |
| Схема БД | `alembic check` | расхождений нет |
| Браузер | `npm run build && npx playwright test` | **2 пройдено** (2.3 с и 2.6 с) |

Документация обновлена: README §9 (числа 59 и 29, мутации переписанных
проверок), таблица наполнения по факту (П2.4), обязательность пересборки
перед браузерным контуром (П2.2).

## П3.5 П2.5: разбор 13 методов без связей с инструментами

Снято с заполненной базы. Все 13 — опубликованные методы:

| Метод | Функция |
|---|---|
| `ai_director_pacing` | Поведенческий ИИ NPC |
| `audio_convolution_reverb` | Аудиосистема |
| `cloth_baked_animation`, `cloth_constraint_simulation` | Симуляция ткани |
| `runtime_fracture_budget` | Разрушения |
| `hair_cards_lod`, `hair_strand_simulation` | Волосы |
| `lag_compensation_rewind`, `subtick_networking` | Сетевой код |
| `directstorage_io` | Потоковая загрузка |
| `portal_scene_capture_budget` | Порталы |
| `async_compute_overlap` | Архитектура рендера |
| `runtime_security_budget` | Античит |

**Отсутствие связи у всех 13 обосновано, данные не потеряны.** Это решения,
которые не опираются на встроенный инструмент конкретного движка: сетевой код
и античит реализуются поверх движка, DirectStorage — платформенный API,
а остальные — приёмы уровня движка в целом. Показательно, что
`directstorage_io` вообще не должен иметь связи с инструментом: его условие —
платформа, и оно уже проверяется тестом `test_platform_condition_directstorage`.

Значит, настоящий дефект не в данных, а в неразличимости состояний:
`engine_support = null` сейчас означает и «метод не зависит от инструмента
движка», и «данных нет». Пользователь не может их отличить, а предупреждение
сида («нет связей с инструментами») перестанет быть правдой, если объявить
все такие методы независимыми.

Требует предметного решения, а не правки проверок. Варианты:

1. Явный признак «не зависит от инструментов движка» в данных метода
   (поле + миграция + пояснение в карточке). Честно, но меняет схему.
2. Дописать связи там, где аналог в движке действительно есть
   (ткань, волосы, разрушения), остальных пометить признаком из п. 1.
3. Оставить как есть, но показывать в карточке «привязка к версии движка
   не указана» вместо пустоты — минимально и ничего не утверждает.

Без выбора предметной позиции правка превратилась бы в догадку, поэтому
здесь изменений не внесено. Решение принято в части 4 (вариант 1) — см. П4.1.

## П3.6 Прочее, оставленное без исправления

* **П2.3, смежное**: `POST` на неизвестный путь даёт корректный 405 —
  это оставлено как есть, проблема была только в GET.

---

# Часть 4. П3.5: признак независимости от инструмента движка

## П4.1 Предметное решение

Выбран вариант 1 из П3.5: явный признак в данных метода. Основание —
независимость от инструмента движка относится к самим данным (это свойство
решения), а не к способу показа. Вариант 3 («не указана») не устранял бы
главного: он честен, но заставляет пользователя гадать, почему у метода
нет привязки, — и для 13 методов из 124 это регулярный вопрос. Вариант 2
смешивал бы два разных утверждения: дописанные связи (ткань, волосы,
разрушения) означали бы «в движке есть близкий аналог», тогда как у этих
решений он именно частичный, и это уже выражено типом связи.

Признак вводится как `False` по умолчанию: отсутствие связей по-прежнему
считается пробелом, пока независимость не обоснована. Перечень — один список
`ENGINE_TOOL_INDEPENDENT` в `methods_data.py` с критерием у каждого кода,
а не правка каждого описания: часть методов задана позиционными кортежами,
и один список читается лучше.

## П4.2 Что сделано

| Куда | Что |
|---|---|
| Миграция | `b2indep01` (голова вместо `7a1toolvers`): колонка `methods.engine_tool_independent`, `server_default=false` — без него EXISTS-строки получили бы NULL при NOT NULL; снимать его после заполнения нельзя (SQLite не умеет `DROP DEFAULT`) |
| Модель и схемы | `Method.engine_tool_independent`; поле доходит до `MethodOut` и `RecommendationOut` |
| Каталог | список из 13 кодов; `all_methods()` ставит признак централизованно |
| Синхронизация | `correct_engine_tool_independence` — доносит признак до уже существующих баз (колонку добавила миграция со значением «ложь»);_admin-правку вне списка не стирает |
| Проверка целостности | пустая связь без признака — предупреждение; признак при существующих связях — ошибка |
| Интерфейс | карточка: «метод реализуется своими средствами… привязка к версии не требуется»; список рекомендаций: «своё решение поверх движка — встроенный инструмент не нужен» вместо «встроенного аналога нет» |
| Унаследованные базы | `LEGACY_MARKER_CHAIN`: штамп по самому свежему присутствующему маркеру, иначе база предыдущей ревизии штамповалась бы слишком рано и upgrade падал на повторном добавлении колонки |

## П4.3 Проверки и мутации

Четыре теста в `test_engine_versions.py` (backend **59 → 63**):

| Тест | Дефект |
|---|---|
| `test_unlinked_methods_are_explicitly_engine_independent` | каждое отсутствие связей объяснено признаком; признак не проставлен методам со связями |
| `test_recommendation_names_independence_not_gap` | элемент рекомендации не отличает «не нужен инструмент» от «данных нет» |
| `test_seed_validation_distinguishes_independence` | правило целостности снова считает осознанное отсутствие связей пробелом; противоречие «признак есть и связи есть» не замечается |
| `test_independence_sync_repairs_existing_database` | синхронизация не доходит до существующих баз или стирает чужое значение |

Мутации (каждая вносилась в продукт и откатывалась из резервной копии —
рабочее дерево содержит незафиксированные правки, поэтому `git checkout --`
был бы потерей работы):

| № | Внесённая ошибка | Где | Упавшие тесты |
|---|---|---|---|
| 1 | Из списка исключён `directstorage_io` | `seed/methods_data.py` | 1-й (множества не совпали), 2-й, 3-й (ложное предупреждение) |
| 2 | Признак не доходит до выдачи | `services/recommender.py` | только 2-й |
| 3 | Правило целостности возвращено к прежнему | `seed/seeder.py` | только 3-й |
| 4 | Синхронизация выравнивает значения по списку, стирая чужое | `seed/corrections.py` | только 4-й |
| 5 | Синхронизация не работает вовсе | `seed/corrections.py` | только 4-й |
| 6 | Признаком помечен метод со связями (`virtual_geometry_clusters`) | `seed/methods_data.py` | 1-й (признак врёт), 3-й (противоречие) |

Перекрытие в мутациях 1 и 6 — не дубль проверок: первый тест ловит
необъяснённое отсутствие связей, третий — правило целостности каталога,
это разные дефекты на одном источнике данных.

## П4.4 Состояние после правок

| Контур | Команда | Результат |
|---|---|---|
| Backend | `pytest -q` | **63 пройдено** |
| Внешняя сверка | `pytest -q` в `validation/` | **6 пройдено** |
| Frontend | `npm test` | **29 пройдено** |
| Типы frontend | `npm run typecheck` | без ошибок (понадобилось добавить поле в тип `Recommendation` и в фикстуру `effectScope.test.tsx`) |
| Схема БД | `alembic check` | расхождений нет |
| Браузер | `npm run build && npx playwright test` | **2 пройдено** |
| Докат старой базы | ручная сверка `ensure_schema` | база структуры `7a1toolvers` штампуется этой ревизией и докатывается до `b2indep01`; свежая база поднимается миграциями |

Документация обновлена: README §9 (63 теста, новые мутации), описание метода
(признак вместо мёртвого `used_in_projects`), удалённая сущность `GameExample`
убрана из таблицы сущностей, абзац о 13 предупреждениях целостности переписан
по факту.
