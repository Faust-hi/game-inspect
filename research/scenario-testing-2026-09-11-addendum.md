# Дополнение к тестированию: покрытие, железо, оценка игр, разбор дефектов

Продолжение отчёта `scenario-testing-2026-09-11.md`. Отвечает на вопросы: нормально ли покрытие решений, как оценка железа соотносится с реальными требованиями, как проект оценил каждую игру (включая ограничения) и насколько серьёзен каждый из восьми найденных дефектов.

## 1. Покрытие решений: нормально ли это

**Числа.** 155 игровых кейсов; привязку к функциям имеют 46 (44 «покрыт» + 2 «частично»), остальные 109 — reference-реализации и возможности движков. 40 функций, 124 методов; у 22 функций по 1–2 метода, у 1 — ни одного.

**Вердикт: для учебного MVP — нормально, для рабочего инструмента — нет.** Что сделано хорошо: каждая функция закрыта хотя бы одним методом (кроме одной), и у методов есть доказательства. Что не нормально:

- **Одна функция без методов вообще** — `storage_streaming` (объявлена, но методов нет — выбрать способ реализации нельзя). Пользователь может выбрать её в профиле и получить пустую выдачу без объяснения.
- **Крайне неравномерное покрытие.** У `multiplayer_netcode` и `open_world_streaming` по 8 методов, у `geometry_pipeline`, `render_scalability`, `runtime_memory`, `project_architecture`, `split_screen_rendering`, `upscaling_frame_generation`, `portal_rendering`, `runtime_security` — по одному. Там, где метод один, «рекомендация» вырождается: выбирать не из чего, а ранжирование по TOPSIS теряет смысл.
- **Половина каталога не привязана к функциям.** 109 из 155 кейсов — это reference-реализации («Chunked LOD reference implementation», «CMU motion capture database») и возможности движков. Как источники они полезны, но в «Сверку с практикой» не попадут и покрытие решений не увеличивают.
- **Дубликаты кейсов.** Left 4 Dead встречается трижды, DOOM Eternal — трижды, Overwatch, Cyberpunk 2077, Horizon Zero Dawn — по два-три раза. Это раздувает счётчик кейсов, но не добавляет доказательств.

**Итог:** покрытие закрывает «широкие» функции, но не выдерживает глубины: на типовой профиль приходится 1–3 реальных альтернативы. Для учебного проекта этого достаточно, чтобы показать механику выбора; для продакшена — нет.

## 2. Оценка железа против реальных требований

Реальные требования — документированные спецификации издателя (минимум и рекомендованные) на релизе игры или её PC-порта. Проект считает «минимальный современный класс» под заявленную цель 1080p/высокое/60 FPS (для отдельных сценариев — другую), а не воспроизводит требования релиза. Поэтому сравнивать нужно направление и масштаб разницы, а не точное совпадение.

| № | Игра | Проект: GPU-ориентир | Реальная рек. GPU | Проект: RAM | Реальная рек. RAM | Разница |
| --- | --- | --- | --- | ---: | ---: | --- |
| S01 | Left 4 Dead | Radeon RX 580 | GeForce 7600 / Radeon X1600 | 11.1 ГБ | 2 ГБ | см. ниже |
| S02 | Counter-Strike 2 | GeForce RTX 3060 Ti | — | 9.7 ГБ | — | см. ниже |
| S03 | Portal 2 | GeForce GTX 1660 Super | — | 10.1 ГБ | — | см. ниже |
| S04 | It Takes Two | GeForce RTX 3050 | GeForce GTX 980 / R9 290X | 11.4 ГБ | 16 ГБ | см. ниже |
| S05 | Split Fiction | GeForce RTX 5060 Ti | RTX 2070 Super / RX 6700 XT | 13.5 ГБ | 16 ГБ | см. ниже |
| S06 | Unreal Engine City Sample | Arc A770 | GeForce RTX 2080 | 17.7 ГБ | 64 ГБ | см. ниже |
| S07 | Fortnite | GeForce RTX 3060 | GeForce GTX 960 / R9 280 | 15 ГБ | 16 ГБ | см. ниже |
| S08 | VALORANT | GeForce GTX 1660 | GeForce GTX 1050 Ti | 9.7 ГБ | 16 ГБ | см. ниже |
| S09 | DOOM Eternal | Arc A750 | GeForce GTX 1080 / RX Vega 56 | 12.1 ГБ | 16 ГБ | см. ниже |
| S10 | Cyberpunk 2077 | GeForce RTX 5060 Ti | GeForce GTX 1060 6 ГБ / RX 580 | 17.3 ГБ | 12 ГБ | см. ниже |
| S11 | No Man's Sky | GeForce RTX 3060 | GeForce GTX 960 | 16.2 ГБ | 8 ГБ | см. ниже |
| S12 | Microsoft Flight Simulator | GeForce RTX 3060 | GeForce RTX 2080 / RX 5700 XT | 15 ГБ | 32 ГБ | см. ниже |
| S13 | Teardown | Radeon RX 6600 | — | 12.8 ГБ | — | см. ниже |
| S14 | BeamNG.drive | Radeon RX 580 | GeForce GTX 970 | 12.1 ГБ | 8 ГБ | см. ниже |
| S15 | Ashes of the Singularity | GeForce RTX 3060 | GeForce GTX 970 | 14.4 ГБ | 16 ГБ | см. ниже |
| S16 | Horizon Zero Dawn | GeForce RTX 3060 | GeForce GTX 1060 6 ГБ / RX 480 | 13.8 ГБ | 16 ГБ | см. ниже |
| S17 | Alan Wake 2 | GeForce RTX 4070 | GeForce RTX 3070 / RX 6800 XT | 12.6 ГБ | 16 ГБ | см. ниже |
| S18 | F.E.A.R. | GeForce GTX 1050 Ti | GeForce 6800 / Radeon X800 | 9.6 ГБ | 1 ГБ | см. ниже |
| S19 | Uncharted 4: A Thief's End | Radeon RX 580 | GeForce GTX 1060 6 ГБ / RX 480 | 11.3 ГБ | 16 ГБ | см. ниже |
| S20 | Red Faction: Guerrilla | GeForce RTX 3060 | — | 15.7 ГБ | — | см. ниже |
| S21 | Minecraft | GeForce RTX 3060 | GeForce GTX 700 / RX 200 | 15.2 ГБ | 8 ГБ | см. ниже |
| S22 | Hunt: Showdown | GeForce RTX 3060 | GeForce GTX 1070 / RX 590 | 12.9 ГБ | 12 ГБ | см. ниже |
| S23 | V Rising | Radeon RX 580 | GeForce GTX 1070 / RX 580 | 11.9 ГБ | 12 ГБ | см. ниже |
| S24 | Brotato | GeForce GTX 1060 6GB | — | 10.5 ГБ | — | см. ниже |
| S25 | Star Wars: The Old Republic | Radeon RX 580 | GeForce 8800 / Radeon HD 3850 | 14 ГБ | 4 ГБ | см. ниже |

**Что видно из таблицы.** Направление ошибки почти всегда одно — **проект оценивает требования выше реальных**. Причины три, и все они системные:

1. **Каталог железа охватывает 2013–2025 годы.** Для игры 2005–2011 годов исторических карт просто нет, поэтому ориентиром становится современная карта минимального класса. F.E.A.R. (2005) получает GTX 1050 Ti, Left 4 Dead (2008) — RX 580, Portal 2 (2011) — RX 6500 XT.
2. **Фиксированные накладные расходы.** Модель добавляет постоянный запас на ОС, драйверы и движок, поэтому даже Brotato (реально 2 ГБ RAM, любая DX11-карта) получает 10.8 ГБ RAM и GTX 1060, а Minecraft — 14.7 ГБ против реальных 8 ГБ.
3. **Другая цель, чем у издателя.** Издатель публикует требования под ~1080p/30–60 FPS на низких-средних настройках. Проект целится в 1080p/высокое/60 FPS, а CS2 — в 240 FPS. Отсюда разрыв в разы.

**Где проект попадает в реальность.** На современных AAA-играх сценарии сходятся куда ближе: Alan Wake 2 — RTX 4070 против реальных RTX 3070 (рекомендованные) и RTX 4080 (трассировка пути); Cyberpunk 2077 — RTX 5060 Ti против реальных RTX 3070/4070 для RT; Split Fiction — RTX 5060 Ti против RTX 2070 Super. Это ожидаемо: чем ближе эпоха игры к эпохе каталога, тем меньше искажение.

**Где проект ломается.** Counter-Strike 2: требуемый CPU-индекс 1.05 превышает максимум каталога (1.0 у i9-14900K), и ориентир по процессору **не выдаётся вовсе** — при том что Valve заявляет минимумом i5-750. DOOM Eternal: GPU-ориентир не выдан из-за требования «Compute Shaders». То есть на двух сценариях из 25 главный результат — пустой.

**Вывод по железу.** Как «какой класс ПК нужен сегодня под такое ТЗ» — оценка осмысленна и внутренне согласована. Как «сравнение с реальными требованиями игры» — некорректна по построению, и это стоит явно писать в интерфейсе, а не оставлять пользователю догадываться, почему Portal 2 «требует» RX 6500 XT.

## 3. Как проект оценил каждую игру (и её ограничения)

Для каждого сценария: вердикт проекта, узкое место и **ограничения**, которые проект сам объявил — в том числе «слишком сильно нагружает».

### S01. Left 4 Dead

- **Вердикт:** узкое место — CPU (главный поток); класс CPU 2, GPU 2; уверенность 0.61; накопитель `sata_ssd`; оценка вызовов отрисовки 25164.
- **Направление нагрузки:** CPU 47, GPU 50, RAM 50, VRAM 50 (50 — без изменений).
- **Ограничения игры:**
  - «Распределение бюджета обновлений агентов»: ожидаемое снижение качества изображения или анимации (-1).
  - «Распределение бюджета обновлений агентов»: может потребовать изменения концепции (-1).
  - «Поиск пути с распределением по кадрам»: может потребовать изменения концепции (-1).
  - «Контроллер популяции NPC по интенсивности событий»: может потребовать изменения концепции (-1).

### S02. Counter-Strike 2

- **Вердикт:** узкое место — CPU (главный поток); класс CPU 5, GPU 3; уверенность 0.4; накопитель `sata_ssd`; оценка вызовов отрисовки 17375.
- **Направление нагрузки:** CPU 51, GPU 50, RAM 50, VRAM 50 (50 — без изменений).
- **Ограничения игры:**
  - Целевой FPS 240 выше откалиброванного диапазона (30–144): стоимость кадра экстраполирована.
  - Позднее внедрение затруднено: «Бюджетирование серверного тикрейта», «Временные метки ввода внутри сетевого такта», «Выделенный сервер без графики», «Предсказание на клиенте и реконсиляция».

### S03. Portal 2

- **Вердикт:** узкое место — CPU (главный поток); класс CPU 1, GPU 2; уверенность 0.55; накопитель `sata_ssd`; оценка вызовов отрисовки 17375.
- **Направление нагрузки:** CPU 56, GPU 58, RAM 50, VRAM 53 (50 — без изменений).
- **Ограничения игры:**
  - Позднее внедрение затруднено: «Бюджет плоских отражений».

### S04. It Takes Two

- **Вердикт:** узкое место — CPU (главный поток); класс CPU 3, GPU 2; уверенность 0.4; накопитель `sata_ssd`; оценка вызовов отрисовки 47912.
- **Направление нагрузки:** CPU 50, GPU 44, RAM 50, VRAM 51 (50 — без изменений).
- **Ограничения игры:**
  - Количественный прогноз доступен только для Windows/Linux ПК: для платформ (ps4, xbox_one) совместимый аппаратный прогноз не обещается.
  - «Временное масштабирование изображения»: ожидаемое снижение качества изображения или анимации (-1).
  - «Динамическое разрешение рендера»: ожидаемое снижение качества изображения или анимации (-1).
  - «Выборочное применение экранных эффектов»: ожидаемое снижение качества изображения или анимации (-1).
  - «Бюджетирование split-screen»: может потребовать изменения концепции (-1).

### S05. Split Fiction

- **Вердикт:** узкое место — CPU (главный поток); класс CPU 3, GPU 3; уверенность 0.4; накопитель `sata_ssd`; оценка вызовов отрисовки 66531.
- **Направление нагрузки:** CPU 50, GPU 45, RAM 50, VRAM 50 (50 — без изменений).
- **Ограничения игры:**
  - Количественный прогноз доступен только для Windows/Linux ПК: для платформ (ps5, xbox_series) совместимый аппаратный прогноз не обещается.
  - «Временное масштабирование изображения»: ожидаемое снижение качества изображения или анимации (-1).
  - «Выборочное применение экранных эффектов»: ожидаемое снижение качества изображения или анимации (-1).
  - «Бюджетирование split-screen»: может потребовать изменения концепции (-1).
  - Выбранные решения снижает сетевой трафик (суммарная оценка -1).

### S06. Unreal Engine City Sample

- **Вердикт:** узкое место — GPU (растеризация и трассировка суммарно); класс CPU 1, GPU 3; уверенность 0.46; накопитель `nvme`; оценка вызовов отрисовки 35638.
- **Направление нагрузки:** CPU 38, GPU 47, RAM 50, VRAM 54 (50 — без изменений).
- **Ограничения игры:**
  - Число NPC (35 000) выше верхней границы модели (10 000): оценка не различает значения внутри этой области, результат является нижней границей диапазона.
  - Количественный прогноз доступен только для Windows/Linux ПК: для платформ (ps5, xbox_series) совместимый аппаратный прогноз не обещается.
  - «Иерархические LOD (HLOD)»: ожидаемое снижение качества изображения или анимации (-1).
  - «Виртуализированная геометрия (кластеризованный LOD)»: может потребовать изменения концепции (-1).
  - «Временное масштабирование изображения»: ожидаемое снижение качества изображения или анимации (-1).
  - Позднее внедрение затруднено: «Асинхронная загрузка без блокировки кадра», «Виртуализированная геометрия (кластеризованный LOD)», «Виртуальные карты теней (VSM)», «Разбиение мира на ячейки с потоковой загрузкой».

### S07. Fortnite

- **Вердикт:** узкое место — CPU (главный поток); класс CPU 2, GPU 3; уверенность 0.4; накопитель `nvme`; оценка вызовов отрисовки 24008.
- **Направление нагрузки:** CPU 42, GPU 52, RAM 51, VRAM 54 (50 — без изменений).
- **Ограничения игры:**
  - Число игроков (100) выше порога различения (32): сетевой вклад оценён по насыщению.
  - Количественный прогноз доступен только для Windows/Linux ПК: для платформ (ps5, switch, xbox_series) совместимый аппаратный прогноз не обещается.
  - Выбранные решения снижает сетевой трафик (суммарная оценка -3).
  - Позднее внедрение затруднено: «Асинхронная загрузка без блокировки кадра», «Бюджетирование серверного тикрейта», «Виртуальные карты теней (VSM)», «Выделенный сервер без графики».

### S08. VALORANT

- **Вердикт:** узкое место — CPU (главный поток); класс CPU 3, GPU 2; уверенность 0.55; накопитель `sata_ssd`; оценка вызовов отрисовки 17375.
- **Направление нагрузки:** CPU 51, GPU 50, RAM 50, VRAM 50 (50 — без изменений).
- **Ограничения игры:**
  - Позднее внедрение затруднено: «Бюджетирование серверного тикрейта», «Временные метки ввода внутри сетевого такта», «Выделенный сервер без графики», «Предсказание на клиенте и реконсиляция».

### S09. DOOM Eternal

- **Вердикт:** узкое место — GPU (растеризация и трассировка суммарно); класс CPU 1, GPU 2; уверенность 0.46; накопитель `sata_ssd`; оценка вызовов отрисовки 27228.
- **Направление нагрузки:** CPU 47, GPU 49, RAM 50, VRAM 50 (50 — без изменений).
- **Ограничения игры:**
  - Количественный прогноз доступен только для Windows/Linux ПК: для платформ (ps4, xbox_one) совместимый аппаратный прогноз не обещается.
  - Позднее внедрение затруднено: «Перекрытие графики и асинхронных вычислений GPU», «Предкомпиляция и прогрев PSO».

### S10. Cyberpunk 2077

- **Вердикт:** узкое место — GPU (растеризация и трассировка суммарно); класс CPU 1, GPU 3; уверенность 0.46; накопитель `nvme`; оценка вызовов отрисовки 43575.
- **Направление нагрузки:** CPU 45, GPU 40, RAM 51, VRAM 54 (50 — без изменений).
- **Ограничения игры:**
  - Количественный прогноз доступен только для Windows/Linux ПК: для платформ (ps5, xbox_series) совместимый аппаратный прогноз не обещается.
  - «Временное масштабирование изображения»: ожидаемое снижение качества изображения или анимации (-1).
  - «Динамическое разрешение рендера»: ожидаемое снижение качества изображения или анимации (-1).
  - В расчёте есть трассировка лучей: требуется видеокарта с аппаратной поддержкой RT, производительность проверяется по отдельной RT-шкале.
  - Позднее внедрение затруднено: «Асинхронная загрузка без блокировки кадра», «Виртуальные карты теней (VSM)», «Выборочная трассировка отдельных эффектов», «Разбиение мира на ячейки с потоковой загрузкой».

### S11. No Man's Sky

- **Вердикт:** узкое место — CPU (главный поток); класс CPU 2, GPU 3; уверенность 0.4; накопитель `nvme`; оценка вызовов отрисовки 44222.
- **Направление нагрузки:** CPU 44, GPU 50, RAM 50, VRAM 52 (50 — без изменений).
- **Ограничения игры:**
  - Количественный прогноз доступен только для Windows/Linux ПК: для платформ (ps4, xbox_one) совместимый аппаратный прогноз не обещается.
  - Позднее внедрение затруднено: «Асинхронная загрузка без блокировки кадра», «Асинхронные инкрементальные автосейвы», «Сдвиг начала координат (origin rebasing)», «Чанковая генерация процедурного ландшафта».

### S12. Microsoft Flight Simulator

- **Вердикт:** узкое место — GPU (растеризация и трассировка суммарно); класс CPU 2, GPU 3; уверенность 0.4; накопитель `nvme`; оценка вызовов отрисовки 25882.
- **Направление нагрузки:** CPU 39, GPU 49, RAM 48, VRAM 41 (50 — без изменений).
- **Ограничения игры:**
  - Количественный прогноз доступен только для Windows/Linux ПК: для платформ (xbox_series) совместимый аппаратный прогноз не обещается.
  - Позднее внедрение затруднено: «GPU-инстансинг растительности», «Асинхронная загрузка без блокировки кадра», «Виртуальное текстурирование», «Разбиение мира на ячейки с потоковой загрузкой».

### S13. Teardown

- **Вердикт:** узкое место — GPU (растеризация и трассировка суммарно); класс CPU 2, GPU 2; уверенность 0.55; накопитель `sata_ssd`; оценка вызовов отрисовки 17363.
- **Направление нагрузки:** CPU 40, GPU 49, RAM 52, VRAM 52 (50 — без изменений).
- **Ограничения игры:**
  - «LOD физики и спящие тела»: ожидаемое снижение качества изображения или анимации (-1).
  - «LOD физики и спящие тела»: может потребовать изменения концепции (-1).
  - «Запечённые кэши разрушений»: может потребовать изменения концепции (-1).
  - В расчёте есть трассировка лучей: требуется видеокарта с аппаратной поддержкой RT, производительность проверяется по отдельной RT-шкале.

### S14. BeamNG.drive

- **Вердикт:** узкое место — CPU (главный поток); класс CPU 2, GPU 2; уверенность 0.55; накопитель `sata_ssd`; оценка вызовов отрисовки 23956.
- **Направление нагрузки:** CPU 46, GPU 50, RAM 50, VRAM 50 (50 — без изменений).
- **Ограничения игры:**
  - «Фиксированный шаг физики с интерполяцией»: может потребовать изменения концепции (-1).
  - «LOD физики и спящие тела»: ожидаемое снижение качества изображения или анимации (-1).
  - «LOD физики и спящие тела»: может потребовать изменения концепции (-1).
  - «Уровни детализации симуляции транспорта»: ожидаемое снижение качества изображения или анимации (-1).

### S15. Ashes of the Singularity

- **Вердикт:** узкое место — CPU (главный поток); класс CPU 2, GPU 3; уверенность 0.61; накопитель `sata_ssd`; оценка вызовов отрисовки 41719.
- **Направление нагрузки:** CPU 47, GPU 50, RAM 50, VRAM 50 (50 — без изменений).
- **Ограничения игры:**
  - «Распределение бюджета обновлений агентов»: ожидаемое снижение качества изображения или анимации (-1).
  - «Распределение бюджета обновлений агентов»: может потребовать изменения концепции (-1).
  - «Поиск пути с распределением по кадрам»: может потребовать изменения концепции (-1).
  - «Поле направлений (flow field)»: ожидаемое снижение качества изображения или анимации (-1).

### S16. Horizon Zero Dawn

- **Вердикт:** узкое место — CPU (главный поток); класс CPU 1, GPU 3; уверенность 0.46; накопитель `nvme`; оценка вызовов отрисовки 21569.
- **Направление нагрузки:** CPU 39, GPU 42, RAM 50, VRAM 49 (50 — без изменений).
- **Ограничения игры:**
  - Количественный прогноз доступен только для Windows/Linux ПК: для платформ (ps4) совместимый аппаратный прогноз не обещается.
  - «Импосторы и билборды для дальнего плана»: ожидаемое снижение качества изображения или анимации (-1).
  - «Импосторы и билборды для дальнего плана»: может потребовать изменения концепции (-1).
  - «Атласы и запечённые LOD растительности»: ожидаемое снижение качества изображения или анимации (-1).
  - Позднее внедрение затруднено: «GPU-инстансинг растительности», «Асинхронная загрузка без блокировки кадра», «Разбиение мира на ячейки с потоковой загрузкой», «Скиннинг на GPU».

### S17. Alan Wake 2

- **Вердикт:** узкое место — GPU (растеризация и трассировка суммарно); класс CPU 1, GPU 4; уверенность 0.4; накопитель `sata_ssd`; оценка вызовов отрисовки 26713.
- **Направление нагрузки:** CPU 50, GPU 40, RAM 50, VRAM 51 (50 — без изменений).
- **Ограничения игры:**
  - Количественный прогноз доступен только для Windows/Linux ПК: для платформ (ps5, xbox_series) совместимый аппаратный прогноз не обещается.
  - «Временное масштабирование изображения»: ожидаемое снижение качества изображения или анимации (-1).
  - «Бюджет выборок и денойзинг трассировки пути»: ожидаемое снижение качества изображения или анимации (-1).
  - В расчёте есть трассировка лучей: требуется видеокарта с аппаратной поддержкой RT, производительность проверяется по отдельной RT-шкале.

### S18. F.E.A.R.

- **Вердикт:** узкое место — CPU (главный поток); класс CPU 1, GPU 1; уверенность 0.61; накопитель `sata_ssd`; оценка вызовов отрисовки 16426.
- **Направление нагрузки:** CPU 48, GPU 50, RAM 50, VRAM 50 (50 — без изменений).
- **Ограничения игры:**
  - «Поиск пути с распределением по кадрам»: может потребовать изменения концепции (-1).
  - «Бюджет восприятия NPC»: ожидаемое снижение качества изображения или анимации (-1).
  - Позднее внедрение затруднено: «Деревья поведений с бюджетом обновления».

### S19. Uncharted 4: A Thief's End

- **Вердикт:** узкое место — CPU (главный поток); класс CPU 1, GPU 2; уверенность 0.4; накопитель `sata_ssd`; оценка вызовов отрисовки 23956.
- **Направление нагрузки:** CPU 49, GPU 46, RAM 50, VRAM 50 (50 — без изменений).
- **Ограничения игры:**
  - Количественный прогноз доступен только для Windows/Linux ПК: для платформ (ps4) совместимый аппаратный прогноз не обещается.
  - Позднее внедрение затруднено: «Скиннинг на GPU».

### S20. Red Faction: Guerrilla

- **Вердикт:** узкое место — CPU (главный поток); класс CPU 2, GPU 3; уверенность 0.4; накопитель `nvme`; оценка вызовов отрисовки 36935.
- **Направление нагрузки:** CPU 43, GPU 50, RAM 54, VRAM 53 (50 — без изменений).
- **Ограничения игры:**
  - Количественный прогноз доступен только для Windows/Linux ПК: для платформ (ps4, xbox_one) совместимый аппаратный прогноз не обещается.
  - «Фиксированный шаг физики с интерполяцией»: может потребовать изменения концепции (-1).
  - «LOD физики и спящие тела»: ожидаемое снижение качества изображения или анимации (-1).
  - «LOD физики и спящие тела»: может потребовать изменения концепции (-1).
  - «Запечённые кэши разрушений»: может потребовать изменения концепции (-1).

### S21. Minecraft

- **Вердикт:** узкое место — CPU (главный поток); класс CPU 2, GPU 3; уверенность 0.55; накопитель `nvme`; оценка вызовов отрисовки 44222.
- **Направление нагрузки:** CPU 44, GPU 50, RAM 51, VRAM 52 (50 — без изменений).
- **Ограничения игры:**
  - Позднее внедрение затруднено: «Асинхронная загрузка без блокировки кадра», «Асинхронные инкрементальные автосейвы», «Чанковая генерация процедурного ландшафта».

### S22. Hunt: Showdown

- **Вердикт:** узкое место — GPU (растеризация и трассировка суммарно); класс CPU 2, GPU 3; уверенность 0.61; накопитель `sata_ssd`; оценка вызовов отрисовки 25739.
- **Направление нагрузки:** CPU 46, GPU 50, RAM 51, VRAM 51 (50 — без изменений).
- **Ограничения игры:**
  - «Потоковое сжатие аудио»: ожидаемое снижение качества изображения или анимации (-1).
  - Мультиплеер включён, но решения сетевого кода не выбраны: сетевой вклад учтён по базовому профилю.
  - Позднее внедрение затруднено: «Асинхронная загрузка без блокировки кадра», «Разбиение мира на ячейки с потоковой загрузкой».

### S23. V Rising

- **Вердикт:** узкое место — CPU (главный поток); класс CPU 2, GPU 2; уверенность 0.61; накопитель `sata_ssd`; оценка вызовов отрисовки 26281.
- **Направление нагрузки:** CPU 46, GPU 45, RAM 49, VRAM 49 (50 — без изменений).
- **Ограничения игры:**
  - «Инстансинг и импосторы для толпы»: ожидаемое снижение качества изображения или анимации (-1).
  - «Инстансинг и импосторы для толпы»: может потребовать изменения концепции (-1).
  - «Распределение бюджета обновлений агентов»: ожидаемое снижение качества изображения или анимации (-1).
  - «Распределение бюджета обновлений агентов»: может потребовать изменения концепции (-1).

### S24. Brotato

- **Вердикт:** узкое место — CPU (главный поток); класс CPU 2, GPU 2; уверенность 0.46; накопитель `hdd`; оценка вызовов отрисовки 25497.
- **Направление нагрузки:** CPU 44, GPU 47, RAM 49, VRAM 45 (50 — без изменений).
- **Ограничения игры:**
  - Количественный прогноз доступен только для Windows/Linux ПК: для платформ (android, ios, switch) совместимый аппаратный прогноз не обещается.
  - «Атласные частицы со сниженной точностью»: ожидаемое снижение качества изображения или анимации (-1).
  - «Массовая отрисовка 2D-агентов одним батчем»: может потребовать изменения концепции (-1).

### S25. Star Wars: The Old Republic

- **Вердикт:** узкое место — CPU (главный поток); класс CPU 2, GPU 2; уверенность 0.61; накопитель `nvme`; оценка вызовов отрисовки 35325.
- **Направление нагрузки:** CPU 50, GPU 50, RAM 50, VRAM 50 (50 — без изменений).
- **Ограничения игры:**
  - Число игроков (100) выше порога различения (32): сетевой вклад оценён по насыщению.
  - Выбранные решения снижает сетевой трафик (суммарная оценка -3).
  - Позднее внедрение затруднено: «Асинхронные инкрементальные автосейвы», «Бюджетирование серверного тикрейта», «Выделенный сервер без графики», «Предсказание на клиенте и реконсиляция».

## 4. Разбор восьми пунктов: проблема или нет

### Пункт 1. Ранжирование систематически понижает определяющие техники

**Проблема средней тяжести, но концептуальная.** Не баг, а следствие выбранной функции полезности: TOPSIS максимизирует «эффект на кадр / стоимость и риск». Для DSS это неверная постановка — она отвечает на вопрос «что дешевле внедрить», а пользователь спрашивает «как реализовать функцию». Особенно опасно для соревновательных игр: sub-tick в CS2 не улучшает FPS, поэтому честно проигрывает headless-серверу, хотя именно он делает игру CS2. Тяжесть средняя: числа корректны, а приоритеты сдвинуты.

### Пункт 2. 53% выбранных техник не доходит до расчёта

**Настоящая проблема, высокая тяжесть.** Это дефект корректности, а не оформления. Пользователь видит нейтральные 50/50 и не может понять, что расчёт пуст. В Horizon Zero Dawn и Uncharted 4 не учтена ни одна техника — итоговое число оборудования посчитано для пустой корзины. Правильное поведение: либо тянуть обязательные зависимости автоматически, либо запрещать добавление без них, либо показывать нагрузку как «не рассчитано», а не как нейтральную.

### Пункт 3. Противоречивые рёбра каталога

**Настоящий дефект данных, средняя тяжесть.** Пара `motion_matching`/`animation_lod_budget` одновременно «жёстко несовместима» и «обязательно зависима». Это ошибка наполнения, а не кода: движок честно применяет оба правила и выдаёт взаимоисключающие советы. Исправляется правкой данных; класс дефекта опасен тем, что такие пары могут накапливаться.

### Пункт 4. Тип связи `dependency` не распознаётся проверкой корзины

**Настоящий дефект, средняя тяжесть.** Тип объявлен в модели предметной области и используется в расчёте нагрузки, но проверка совместимости корзины его не знает и пишет «не распознан». Пользователь получает противоречивую картину: зависимость учтена в одном месте и не учтена в другом. Правка локальная, но это признак рассинхронизации обработчиков типов.

### Пункт 5. Словарь аппаратных возможностей не покрывает «Compute Shaders»

**Настоящий дефект, высокая тяжесть (для затронутых профилей).** Требование базовой возможности, которую поддерживает любая карта каталога, не может быть подтверждено — и вся оценка оборудования обнуляется. Один метод в корзине (`gpu_particle_simulation`, `gerstner_fft_water`) уничтожает результат. Это не «честная неизвестность», а дыра в словаре: вычислительные шейдеры есть у всех GPU каталога, и их отсутствие в `hw_features` — пробел данных.

### Пункт 6. Меш-шейдеры без PS5 в матрице платформ

**Настоящий дефект данных, средняя тяжесть.** PS5 (RDNA2) поддерживает меш-шейдеры, но метод объявлен только для Windows и Xbox Series. Из-за этого Alan Wake 2 — титул, известный применением мешлетов, — теряет эти методы при указании PS5. Ошибка направленная: занижает применимость, а не завышает.

### Пункт 7. Таксономия Nanite: функция не совпадает с кейс-доказательством

**Настоящий дефект данных, средняя тяжесть.** `virtual_geometry_clusters` числится под `large_scale_terrain`, а его доказательство ссылается на `geometry_pipeline`. Пользователь, выбравший «конвейер геометрии», не увидит Nanite. Для DSS, где выбор функции — основной вход, это прямой промах в маршрутизации.

### Пункт 8. Сквозные методы попадают в функции, которых нет в профиле

**Пограничная проблема, низкая тяжесть.** Само по себе сквозное поведение задумано: не нужно выбирать «PSO» отдельной функцией. Но группировка идёт по исходной функции метода, поэтому арена-шутер получает рекомендацию в разделе `open_world_streaming`. Это не ошибка расчёта, а ошибка представления: правильнее показывать такие методы отдельной группой «сквозные», а не внутри чужой функции.

### Сводка по серьёзности

| № | Дефект | Проблема? | Тяжесть | Что делать |
| --- | --- | --- | --- | --- |
| 1 | Понижение определяющих техник | да, концептуальная | средняя | добавить отдельный критерий «соответствие концепции» с большим весом или режим «архитектурные решения» |
| 2 | 53% техник не учтено | да, корректность | высокая | тянуть обязательные зависимости автоматически либо показывать «не рассчитано» вместо 50/50 |
| 3 | Противоречивые рёбра | да, данные | средняя | убрать дубль `hard_conflict`+`dependency` на одну пару |
| 4 | `dependency` не распознан | да, код | средняя | добавить ветку `dependency` (и `unknown`) в проверку корзины |
| 5 | «Compute Shaders» не подтверждается | да, данные | высокая | внести базовые возможности GPU в `hw_features` или считать их поддержанными по умолчанию |
| 6 | Меш-шейдеры без PS5 | да, данные | средняя | добавить `ps5` в `applicable_platforms` мешлет-методов |
| 7 | Nanite не под той функцией | да, данные | средняя | перенести `virtual_geometry_clusters` в `geometry_pipeline` либо связать обе функции |
| 8 | Сквозные методы в чужой функции | пограничная | низкая | вынести сквозные методы в отдельную группу «сквозные оптимизации» |

**Итого:** шесть из восьми пунктов — настоящие дефекты (два высокой тяжести, четыре средней), один концептуальный и один пограничный. Ни один не ломает приложение целиком, но два (пункты 2 и 5) делают главный результат пустым или вводящим в заблуждение на конкретных профилях.
