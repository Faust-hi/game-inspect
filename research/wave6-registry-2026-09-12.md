# Волна 6 — сводный реестр решений (2026-09-12)

Оркестрация: 4 независимых исследования параллельно, затем проверка оркестратором.
Ничего не менялось — все отчёты read-only.

| Агент | Отчёт |
|---|---|
| Проводка параметров | `research/wave6-parameter-wiring-audit-2026-09-12.md` |
| Арифметика и связи | `research/wave6-arithmetic-audit-2026-09-12.md` |
| Тесты | `research/wave6-test-inventory-2026-09-12.md` |
| Мусор и мёртвый код | `research/wave6-cleanup-inventory-2026-09-12.md` |

## Проверка оркестратором (не доверять самоотчёту)

Перепроверено по коду — **все подтвердились**:
`RESOLUTION_FACTOR["2160p"]=3.0` (hardware.py:97); `memory_model` — обрабатывается только
`unified`, а enum содержит `MANAGED` (hardware.py:1723, enums.py:412); пределы RAM/VRAM только
пишут `unmet`, не клампят (hardware.py:2647-2656); фильтр по VRAM стоит **до** выбора карты
(hardware.py:2190); `resource_fit` не принимает `effect_scope` (rules.py:312); `_local_views`
читает `local_view_count` (hardware.py:1067); в `planning.py:314` `profile.stage` даёт только
заметку.

**Имитация профилей** (`tmp/compare_with_reality.py`, 25 игр): медианы **GPU +28.3 %, CPU −3.8 %,
RAM −6.2 %, VRAM +15.0 %** — совпадают с эталоном 2026-09-11. Техники 44/52 (85 %), узкое место
8/8. **Регресса нет** (правок и не было).

## Реестр дефектов

### P0 — сдвигают сами числа железа
| # | Дефект | Где | Измерено |
|---|---|---|---|
| 1 | **Множитель разрешения не по пикселям**: 2160p = 3.0 при отношении пикселей 4.0 | `hardware.py:95-98` → `1282-1283`, `1874-1878` | rgpu 0.4644→0.6066 (**+30.6 %**), VRAM 15.5→18.8 (**+21.3 %**) |
| 2 | **Фильтр VRAM до выбора карты** (обрыв на округлении) | `hardware.py:2190` + `2265` | требование 8.04 ГБ → RX 580 (растр 0.22); 8.05 → RTX 3060 (0.43). **+95 %** растра, класс 2→3 |
| 3 | **`local_view_count` действует без split-screen** | `hardware.py:1061-1071` | cpu_index 0.2469→0.5779 (**+134 %**), gpu +48 %, vram +1.3 ГБ |

### P1 — ложное обещание контракта или тихая инертность
| # | Дефект | Где |
|---|---|---|
| 4 | `memory_model="managed"` **инертен** (равен `dedicated`) — а анкета предлагает «управляемая куча / GC» | `hardware.py:1723`, `enums.py:412`, `ProfileScreen.tsx:202-208` |
| 5 | Пределы RAM/VRAM **не ограничивают**, а блок назван «Обязательные ограничения» | `hardware.py:2647-2656`, `schemas/catalog.py:125` |
| 6 | `resource_fit` игнорирует `effect_scope`: серверный метод получает максимум фита | `rules.py:312-325` |
| 7 | **Два канала влияния методов**, 14 расхождений знака (8 CPU, 6 GPU): карточка метода противоречит арифметике | `hardware.py:324-347/1425` против `methods_data.py:10-11` |
| 8 | `draw_call_budget` — только текст предупреждения | `hardware.py:2473`, `953-970` |
| 9 | **План не зависит от профиля**: ни одно поле не меняет p50/задачи/critical path | `planning.py:314` (единственное чтение) |
| 10 | Формат 2D не снижает нагрузку (2D = 2.5D = 3D) | `hardware.py:940` |
| 11 | Размер установки зависит от разрешения, против собственного комментария | `hardware.py:2364` |

### P2 — формулировки и мелкое
12. Знаковая ошибка текста у 1 метода (`hardware.py:2282-2287` против таблицы `509`).
13. Мёртвый код: `log_event` (`logging_setup.py:35`) — ноль ссылок (проверено дважды).
14. Пробел покрытия R6: нет тестов на «нет доступа к приватным файлам» и «нет записей с чужого origin».

## Очистка: папка `report/`
Дерево ~1522 МБ: **CORE ~333 МБ** (312 из них — окружения `node_modules`/`.venv`/`.dss-venv`),
**→REPORT ~34 МБ**, **JUNK ~1085 МБ (71 %)**.

JUNK, крупное: `research/_mp/` 466 МБ · `tmp/*.db` 212 МБ · `backend/*.bak-*.db` (43 шт.) 203 МБ ·
`.runtime-packages/` 67 МБ · `.git-safety-backup/` 58 МБ · `tmp/n2_options/*.db` 33 МБ ·
`.uv-cache` 12 МБ · `tmp/*.json` 15 МБ.
Раскладка `report/`: `research-docs/{waves,scenarios,audits,session,workqueue}`, `probes/`,
`tools-once/`, `artifacts/`. **`research/packs/` — CORE** (рантайм-данные, `pack_loader.py:33`);
CORE-документы (`category_verification.md`, `reliability-report.md`, `verification_report.*`,
`game-development-dss-study.md`, `catalog-coverage-matrix.md`) остаются на месте — на них ссылаются README/tools.

## Вырезание кейсов и work_packages
~0.6 МБ исходников, ~40 точек: 4 модели (`entities.py:460-602`), 4 миграции,
`evidence_catalog.py` (`CASE_RECORDS`/`TEAM_RECORDS`/`sync_cases`/`sync_work_packages`),
`pack_loader.py`, `repositories.py`, `services/evidence.py`, весь `services/planning.py`,
маршруты `/cases`, `/teams`, `/schedule`, схемы, 4 frontend-экрана (`CasesScreen`, `ScheduleScreen`,
`PlanScreen`, `EvidenceScreen`), 6 тестов, 37 research-файлов.

## Сокращение тестов
Измерено: backend **192 сценария / 24 файла / 40.8 с**; frontend **29 тестов / 7 файлов / 2.9 с**;
e2e 2 сценария.
План: **critical 80** (≈15–22 с) + **extended 80**; удалить **32** (все — снятые требования:
кейсы, пакеты работ, команды). Точных дублей нет. Frontend остаётся целиком в основном прогоне;
e2e — только вручную. Маркеры `critical`/`extended` + `addopts = -m critical` в `backend/pytest.ini`.

## Что требует решения владельца
1. Папка `report/`: имя и расположение (`report/` в корне проекта или рядом с проектом).
2. Вырезание кейсов и work_packages/команд/планов — подтвердить (это удаление кода и миграций).
3. Сокращение тестов по плану — подтвердить (32 удаления, маркеры, CI).
4. Очистка мусора ~1085 МБ — подтвердить (с бэкапом перед удалением).
5. Дефекты P0 (1–3) — чинить в этой волне или отдельной? Пункты 1 и 3 меняют числа железа.
