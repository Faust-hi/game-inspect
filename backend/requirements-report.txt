# Зависимость только инструмента отчёта, не приложения.
#
# `tools/generate_research_report.py` собирает PDF через ReportLab, а backend и
# инструменты аудита в нём не нуждаются. Поэтому пакет вынесен отдельно и не
# входит в `requirements.lock`: иначе его тянула бы каждая установка.
#
# Установка в интерпретатор, которым запускается сборка:
#
#   python -m pip install -r backend/requirements-report.txt
#
# Без него инструмент честно сообщает, что PDF не собран, и всё равно пишет
# Markdown и матрицу покрытия (либо запустите его с `--no-pdf`).
reportlab>=4.0,<5
