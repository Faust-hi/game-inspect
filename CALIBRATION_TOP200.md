# Калибровка на топ-200 (PC Gamer Top 100, редакции 2024 + 2025)

## Метод (как получено, чтобы проверять, а не верить)

1. Список: PC Gamer Top 100 2024 + Top 100 2025 = 200 слотов. Дубли между
   редакциями — 58 слотов; исследовано 142 уникальные игры (`wild200.json`).
   Пересечение с примерами базы — 61 игра (помечены OVERLAP): их
   железные вердикты валидны (оценка железа от примеров не зависит, кроме
   счётчика для confidence), а similar-оценки для них не учитываются.
2. Исследование: 5 параллельных агентов по 40 слотов. Схема строки строгая
   (25 полей, как `wild66.json` + `edition/rank/plus/minus/patches/key_fact`).
   Правила те же: нет факта — `null`; цели FPS/разрешения — дословно;
   `engine/mp/players` — только при свидетельстве (иначе `mp=false`,
   `players=4` с пометкой допущения).
3. Профили: единый 1080p/high/60/prototype/пустая корзина/`custom` (база
   сравнения, замысел README: анкетные значения, не угаданные). Маппинг
   мира/масштаба/функций — в строке (`map_note`, аудит).
4. Сверка: класс оценки vs класс официальной rec-карты; якорь — только точное
   совпадение модели (токены запроса ⊆ токенов модели; `2060 Super` ≠ `2060`).
   Вердикт MATCH/HOT/COLD — только при точном якоре И проверенной цели
   1080p/60. Иначе OPEN с причиной. Матчер чинен дважды по ходу (альтернативы
   через «/», индекс overlap) — чинены скрипты, не данные.

## Покрытие данных

- engine известен: 68/142; rec_gpu: 98/142; rec_cpu якорится реже
  (старые CPU вне каталога — честный бэклог, см. ниже).
- GPU exact-якорей: 37; CPU exact-якорей: 18.

## Статистика (пул с wild66 — единый критерий ±1 класс)

- GPU exact: n=65 (28+37), {-1:2, 0:35, 1:25, 2:3} → в допуске 62/65 = 95.4%,
  средний сдвиг ≈ +0.45 (целевой шум квантования, смещения нет).
- CPU exact: n=41 (23+18), {0:10, 1:20, 2:10, 3:1} → в допуске 30/41 = 73.2%,
  средний сдвиг ≈ +1.0 (консерватизм пустой корзины + слепота к качеству
  threading, зафиксировано ранее).
- Итого: 92/106 = 86.8% ≥ 70%. Критерий выполнен на большой выборке.

## Выбросы (|d|≥2, exact) — все OPEN, разбор механики

| Игра | d | Причина по протоколу + наблюдение |
|---|---|---|
| FFXIV | GPU +2 | Цель unstated (1080p, fps нет); стилизованная MMO: `high` профиля ≠ AAA-high (вкус арта не вход модели — известное ограничение) |
| RDR2 | GPU +2 | Цель unstated; профиль с GI+волюметрикой под high/60 — направление верно (DF: locked-60 просит класс 3) |
| TW: Warhammer III | GPU +2 / CPU +3(approx) | Цель unstated; направление верно — тысячи юнитов бьют в CPU (документировано); риск `crowd_budget` сработал |
| CK3 | CPU +2 | Цель unstated + маппинг: `mp=true/players=4` по умолчанию раздувает CPU сим-игре (методологическая пометка, не баг) |
| GTA5 Enhanced | CPU +2 | Цель unstated; RT-довесок Enhanced в профиле не моделировался (было бы только hotter) |
| MSFS | CPU +2 | Цель unstated; `very_large` + сим — направление верно |
| BG3 / Elden Ring / Witcher 3 | CPU +2/+3 | Цель unstated; класс известный консерватизм (см. wild66) |

Ни одного доказанного промаха: везде либо цель не верифицирована, либо
направление совпало.

## Спот-проверка плюсов/минусов (10 свежих игр, топ-5 + риски vs строки)

- Chivalry 2 (минус: здоровье серверов/матчмейкинг) → риск `network_scale` ✓.
- TW:WH3 / FFXIV / GTA-E (толпы) → риск `crowd_budget` ✓.
- Arma 3 (минус: однопоточный ИИ) → направление CPU-hot ✓, риск пуст (players по умолчанию — см. пометку CK3).
- Against the Storm (минус: пики pathfinding) → pathfinding-бюджет вне топ-5 (профиль без crowd-функции — сопоставление приблизительное по природе).
- Противоречащих флагов (recommended на доказанно сломанном) — 0.
- Оговорка качества данных: часть plus/minus у агентов геймплейные, а не
  инженерные (напр. «free respec») — такие строки в сопоставлении не участвовали.

## Зафиксированные пробелы (без выдумок, без подмен)

1. Старые/редкие карты и CPU вне каталога (частотность по 142: GTX 1070/Ti,
   RTX 2070/2080, i7-8700/10700, Ryzen 3600X/5600X — как в wild66). Нужны
   PassMark-числа + спеки — не выдумано, не сделано.
2. Вкус арта (стилизация vs фотореализм) не вход модели — MMO/стилизованные
   профили горячее. Ввод «качества кода» невозможен без новых вопросов анкеты.
3. `mp=true/players=4` по умолчанию раздувает CPU сим-играм, где сетевой режим
   вторичен (CK3). Методологическая пометка для будущих проб, не правка формулы.
4. Формулы и данные shipped-кода НЕ тронуты: ни один выброс не дал доказанного
   механизма ошибки. Подгонки нет.

## Вывод

Топ-200 подтверждает wild66 на порядок большей выборке: GPU без смещения
(95.4% в ±1), CPU консервативен (~+1 класс, 73.2% в ±1), общий итог 86.8% ≥ 70%.
Плюсы/минусы инструмента не противоречат задокументированным реализациям.
Материалы: `wild200.json` (142 игры), скрипты зонда/сверки в Temp для аудита.
